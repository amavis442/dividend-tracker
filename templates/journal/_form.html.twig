{{ form_start(form, { 'attr': {'autocomplete': 'off', 'data-controller':'ckeditor'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary"><i class="far fa-save"></i> {{ button_label|default('Save')|trans }}</button>
{{ form_end(form) }}
