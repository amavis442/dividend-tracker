{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Journal'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Journal'|trans }}</h1>

    {{ include('journal/_form.html.twig', {'button_label': 'Update'}) }}
    <br/>
    <a href="{{ path('journal_index') }}" class="btn btn-primary"><i class="fas fa-angle-double-left"></i> {{ 'back to list'|trans }}</a>
    <hr/>
    {{ include('journal/_delete_form.html.twig') }}
{% endblock %}

{% block javascripts %}
{{ parent() }}
{{ encore_entry_script_tags('fileupload') }}
{{ encore_entry_script_tags('summernote') }}
{% endblock %}
