{% block menu %}
    
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <ul class="navbar-nav mr-auto">
        {% if is_granted('ROLE_USER') %}
           <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Markets
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a href="{{ url('branch_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Industry</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ url('ticker_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Tickers</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Position
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a href="{{ url('position_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Open positions</a>
                    <div class="dropdown-divider"></div>
                    
                </div>
            </li>
           <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Dividend
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a href="{{ url('payment_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Payments</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ url('calendar_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Calendar</a>
                </div>
            </li>
           <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Reports
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a href="{{ url('report_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Report</a>
                    <a href="{{ url('report_closed_positions_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Closed positions</a>
                    <div class="dropdown-divider"></div>
                    <a href="{{ url('summary_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Summary Tickers</a>
                </div>
            </li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <li class="nav-item">
                <a href="{{ path('app_logout') }}" class="nav-link" >Logout</a>
            </li>
            <li class="nav-item">
                Email: {{ app.user.email }}
            </li>
            {% else %}
            <li class="nav-item">
                <a href="{{ path('app_login') }}" class="nav-link">Login</a>
            </li>
            {% endif %}
            
        </ul>
    </nav>
{% endblock %}