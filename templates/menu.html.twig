{% block menu %}

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="#">Dividend tracker</a>
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown1" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ 'Admin'|trans }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown1">
                        <a href="{{ url('branch_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Industry'|trans }}</a>
                        <a href="{{ url('ticker_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">Tickers</a>
                        <a href="{{ url('research_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Research on Tickers'|trans }}</a>
                        <a href="{{ url('closed_position_index', parameters = {status: 2}, schemeRelative = false) }}" class="dropdown-item">{{ 'Closed positions'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('pie_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Pies'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('csv_import', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Import csv'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('journal_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Journal'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('app_account_update', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Account'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('currency_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Currency'|trans }}</a>
                        <a href="{{ url('tax_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Taxtarifs'|trans }}</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Portfolio
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown2">
                        <a href="{{ url('portfolio_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Portfolio'|trans }}</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown3" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Dividend
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown3">
                        <a href="{{ url('payment_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Payments'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('calendar_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Calendar'|trans }}</a>
                        <a href="{{ url('calendar_per_date', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Calendar per date'|trans }}</a>
                        <a href="{{ url('calendar_per_date_table', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Calendar per date printout'|trans }}</a>
                   </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown4" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        {{ 'Reports'|trans }}
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown4">
                        <a href="{{ url('dividend_tracker', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Dividend Tracker'|trans }}</a>
                        <a href="{{ url('report_allocation_sector', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Sector allocation'|trans }}</a>
                        <a href="{{ url('report_allocation_position', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Position allocation'|trans }}</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ url('dividend_month_index', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Dividend month Tickers'|trans }}</a>
                        <a href="{{ url('report_dividend_projection', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Dividend projection'|trans }}</a>
                        <a href="{{ url('report_payout', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Dividend payout'|trans }}</a>
                        <a href="{{ url('report_dividend_yield', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Dividend yield'|trans }}</a>
                        <a href="{{ url('report_dividend_yield_by_pie', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Dividend yield pie'|trans }}</a>
                        <a href="{{ url('compound_prediction', parameters = [], schemeRelative = false) }}" class="dropdown-item">{{ 'Compound calc'|trans }}</a>
                        <a href="{{ url('report_export', parameters = [], schemeRelative = false) }}" class="dropdown-item" target="_new">{{ 'Export'|trans }}</a>
                      </div>
                </li>

            {% endif %}
        </ul>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="my-2 my-lg-0">
                <span class="navbar-text">Email:
                    {{ app.user.email }}</span>
                <a href="{{ path('app_logout') }}" class="btn btn-primary">{{ 'Logout'|trans }}</a>
            </div>
        {% else %}
            <a href="{{ path('app_login') }}" class="btn btn-primary">{{ 'Login'|trans }}</a>
        {% endif %}


    </nav>
{% endblock %}
