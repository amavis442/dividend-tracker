{% extends 'base.html.twig' %}

{% block title %}New Payment{% endblock %}

{% block body %}
    <h1>Create new Payment: {{ payment.position.ticker.ticker}}</h1>

    {{ include('payment/_form.html.twig') }}
<hr/>
    <a href="{{ path('payment_index') }}" class="btn btn-primary"><i class="fas fa-angle-double-left"></i> back to list</a>
{% endblock %}
