{% extends 'base.html.twig' %}

{% block title %}Payment{% endblock %}

{% block body %}
    <h1>Payment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Payment date</th>
                <td>{{ payment.payDate ? payment.payDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ payment.position.ticker.fullname }} ({{ payment.position.ticker.ticker }})</td>
            </tr>
            <tr>
                <th>Industry</th>
                <td>{{ payment.position.ticker.branch.label }}</td>
            </tr>

            <tr>
                <th>Units</th>
                <td>{{ payment.stocks/100|format_number({fraction_digit: 2})  }}</td>
            </tr>
            <tr>
                <th>Total dividend</th>
                <td>{{ payment.currency.sign }} {{ payment.dividend/100|format_number({fraction_digit: 2}) }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('payment_index') }}" class="btn btn-primary"><i class="fas fa-angle-double-left"></i> back to list</a> | 

    <a href="{{ path('payment_edit', {'id': payment.id}) }}" class="btn btn-primary"><i class="far fa-edit"></i></a>
    <hr/>
    {{ include('payment/_delete_form.html.twig') }}
{% endblock %}
