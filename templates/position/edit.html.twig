{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Position'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Position'|trans }}</h1>

    {{ include('position/_form.html.twig', {'button_label': 'Update'}) }}
    <br/> 
    <a class="btn btn-primary" href="{{ path('portfolio_show',  {'id': position.id}) }}"><i class="fas fa-angle-double-left"></i> {{ 'back to list'|trans }}</a>
    <hr/>
    {{ include('position/_delete_form.html.twig') }}
{% endblock %}
