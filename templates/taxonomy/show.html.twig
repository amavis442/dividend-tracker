{% extends 'base.html.twig' %}

{% block title %}Taxonomy{% endblock %}

{% block body %}
    <h1>Taxonomy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taxonomy.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ taxonomy.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ taxonomy.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_taxonomy_index') }}" class="btn btn-secondary"><i class="fas fa-angle-double-left"></i> {{ 'back to list'|trans }}</a>
    <a href="{{ path('app_taxonomy_edit', {'id': taxonomy.id}) }}" class="btn btn-primary"><i class="far fa-edit"></i> {{ 'edit'|trans }}</a>


    {{ include('taxonomy/_delete_form.html.twig') }}
{% endblock %}
