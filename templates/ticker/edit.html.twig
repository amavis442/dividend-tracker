{% extends 'base.html.twig' %}

{% block title %}
  {{ 'Edit Ticker'|trans }}: {{ ticker.ticker }}
{% endblock %}

{% block body %}
  <h1>{{ 'Edit Ticker'|trans }}: {{ ticker.ticker }}</h1>

  {{ include('ticker/_form.html.twig', { button_label: 'Update' }) }}
  <br />
  <a href="{{ path('ticker_index') }}" class="btn btn-primary"><i class="fas fa-angle-double-left"></i> {{ 'back to list'|trans }}</a>
  <hr />
  {{ include('ticker/_delete_form.html.twig') }}
{% endblock %}
